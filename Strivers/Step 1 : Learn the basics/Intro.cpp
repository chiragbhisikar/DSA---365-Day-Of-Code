#include <bits/stdc++.h>

using namespace std;

int main()
{
    // 	1. User Input / Output
    /*
     * cout -> output & cin -> input
     */

    //  2. Data Type
    /*
     * int, long, long long, float, double
     * string & getline(cin,variable_name)
     * char
     */

    // 3. If Else statements
    /*
     * if
     * if...else
     * if...else if
     * if...else if...else
     */

    // 4.  Switch Statement
    /*
     * switch(conditition){
     *     case n:
     *            break;
     *     case m:
     *            break;
     *     default:
     *            break;
     * }
     */

    // 5.  Array
    /*
     * For Storing Same DataType Multiple Things -> int arr[number_of_elements] -> int arr[row][col]
     */

    // 6.  String
    /*
     * Collection Of Character -> string string_name; -> getline(cin ,string_name)
     */

    // 7.  For loops
    /*
     *  for(int i = 0; i < number_of_time; i++){    }
     *  while(i < number_of_time){  i++;  }
     *  do{     }while(i < number_of_time); -> Run Alteast One Time
     */

    // 8.  Function
    /*
     * return_type function_name(parameters){}
     */

    // Functions (Pass by Reference and Value)
    /*
     *  return_type function_name(int a){} -> pass by value -> a
     *  return_type function_name(int *a){} -> pass by reference -> *a
     */
    return 0;
}